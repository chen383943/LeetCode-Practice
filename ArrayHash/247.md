```{python}
from collections import Counter
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        freq = Counter(nums)
        print(freq)

        bucket = [[] for _ in range(len(nums) + 1)]

        for nums, frequencies in freq.items(): 
            bucket[frequencies].append(nums)
        
        result = []
        for i in range(len(bucket) - 1, -1, -1): 
            for nums in bucket[i]: 
                result.append(nums)
                if len(result) == k: 
                    return result
        return []
```